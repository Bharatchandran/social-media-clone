<%- include('../partials/header')%>




<div class="  border  rounded-lg shadow bg-gray-800 border-gray-700">
    
    <div class="flex flex-col items-center p-8 pb-10">
        <img class="w-24 h-24 mb-3 rounded-full shadow-lg" src="<%=profile.avatar%>" />
        <h5 class="mb-1 text-xl font-medium  text-white"><%=profile.name%></h5>
        <%  if(userId !== currentUserId) {%>
            <div class="flex mt-4 space-x-3 md:mt-6">
                <form action="/messages/<%=userId%>" method="POST">
                <button type="submit" class="text-white   focus:ring-4 font-medium rounded-lg text-sm px-5 py-2.5 mr-2 mb-2 bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-blue-800 ">Message</button>
            </form>
            </div>
            <%  }%>
        
    </div>
    
</div>

<%if(tweets) { %>
    <%  if(userId !== currentUserId) {%>
        <%  }%>
      
            <%- include('../partials/tweetContainer') %>
      
<%}%>
<%- include('../partials/footer')%>